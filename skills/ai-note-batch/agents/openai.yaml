interface:
  display_name: "AI Note Batch"
  short_description: "Sync or batch-review Slax bookmarks for Obsidian"
  default_prompt: "Use $ai-note-batch to sync new Slax Reader bookmarks into the daily Obsidian approval board."
dependencies:
  tools:
    - type: "mcp"
      value: "obsidian"
      description: "Obsidian indexed search, semantic search, metadata, link resolution, and path-specific file tools"
policy:
  allow_implicit_invocation: true
