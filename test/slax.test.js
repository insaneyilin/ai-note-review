import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs/promises'; import os from 'node:os'; import path from 'node:path'; import { listAllInbox } from '../src/slax.js';
test('lists every page',async()=>{ const dir=await fs.mkdtemp(path.join(os.tmpdir(),'reader-')); const cli=path.join(dir,'reader-cli'); await fs.writeFile(cli,`#!/bin/sh\ncase "$*" in *"--page 1"*) printf '{"items":[{"id":"1"},{"id":"2"}],"total":3}' ;; *"--page 2"*) printf '{"items":[{"id":"3"}],"total":3}' ;; esac\n`); await fs.chmod(cli,0o755); const items=await listAllInbox(cli,2); assert.deepEqual(items.map(x=>x.id),['1','2','3']); });
